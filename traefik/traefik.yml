log:
  level: INFO

entryPoints:
  web:
    address: :80
  websecure:
    address: :443

providers:
  docker:
    exposedByDefault: false

certificatesResolvers:
  myresolver:
    acme:
      email: nikak_ne_rak@mail.ru
      storage: /letsencrypt/acme.json
      httpChallenge:
        entryPoint: web
